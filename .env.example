# Non-secret local configuration example. Copy to `.env` to override defaults.
# `.env` is gitignored. No secrets belong here or in `.env`.
APP_ENV=local
APP_VERSION=0.1.0
SCHEMA_VERSION=1
MAX_UPLOAD_MB=250
SESSION_ROOT=.tmp/sessions
SESSION_TTL_HOURS=24
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000
